#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int arr[1000];
   int i,n,s=0;
   scanf("%d\n",&n);
   for(i=0;i<n;i++)
   {
    scanf("%d",&arr[i]);
   }   
   for(i=0;i<n;i++)
   {
    s+=arr[i];
   };
   printf("%d",s);
    return 0;
}
