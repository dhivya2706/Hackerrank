import numpy as np

n, m = map(int, input().split())

matrix = []
for _ in range(n):
    matrix.append(list(map(int, input().split())))

arr = np.array(matrix)

print(np.transpose(arr))

print(arr.flatten())
